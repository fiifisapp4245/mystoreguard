import { TemplateBuilderForm } from "@/components/estimator/template-builder-form"

export const metadata = {
  title: "New template — MyStoreGuard",
}

export default function NewTemplatePage() {
  return <TemplateBuilderForm />
}
