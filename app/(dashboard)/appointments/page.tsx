import { AppointmentsScreen } from "@/components/appointments/appointments-screen"

export const metadata = {
  title: "Appointments — MyStoreGuard",
}

export default function AppointmentsPage() {
  return <AppointmentsScreen />
}
